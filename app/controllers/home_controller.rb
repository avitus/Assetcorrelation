class HomeController < ApplicationController
  def index
    # @news = Rails.cache.fetch('asset-correlation', :expires_in => 24.hours) do 
      # Topsy.search("asset correlation", :perpage => 3, :page => 1, :window => :auto)
    # end
  end
end
